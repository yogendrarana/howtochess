//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "none",
  tabWidth: 2,
  quoteProps: "as-needed",
  printWidth: 100,
  arrowParens: "always",
};

export default config;
